# Include functions that compute elements of S
include("elementsOfS.jl")

# Function that computes the DICs or the sensitivity matrix
function computeDICs(neuron::NeuronCB)
    display("hello")
end


